<%= link_to "戻る", :back, class: 'btn btn-outline-secondary return-btn' %>
<table class="table table-bordered">
  <thead>
    <tr>
      <th scope="col" ><%= @pet.name %></th>
    </tr>
  </thead>
    <%= form_for @condition, url: pet_conditions_path(pet_id: @pet.id), method: :post do |f| %>
    <%= render "devise/shared/error_messages", resource: @condition %>
    <%= render "condition_form", f: f %>
<% end %>
 

<% provide(:title, '管理表編集') %>
<%= link_to "戻る", :back, class: 'btn btn-outline-secondary return-btn' %>
<table class="table table-bordered">
  <thead>
    <tr>
      <th scope="col" ><%= @condition.pet.name %></th>
    </tr>
  </thead>
    <%= form_for @condition, url: condition_path(id: @condition.id) do |f| %>
    <%= render "condition_form", f: f %>
<% end %>
 
